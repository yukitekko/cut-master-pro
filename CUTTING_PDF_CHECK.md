# 切断作業表 PDF保存・共有 確認記録

確認日：2026-08-31

## 今回の範囲

- 選択中の材料について、計算結果からA4横・4列の切断作業表PDFを作成する。
- 既存の印刷、切断総数、1本ごとのチェック欄、2枚目以降の材料名・規格名・ページ数を維持する。
- PDF作成後に「PDFを保存」または「共有先を選ぶ」を押す。共有先へ勝手に送信しない。
- PDFは端末内で作成する。案件データのサーバー送信、クラウド保存は追加しない。
- 共有非対応の場合は「保存後にメールなどへ添付」を案内する。共有キャンセルと失敗は区別する。
- 作成進捗、作成キャンセル、失敗時の再作成に対応する。
- 見積書PDF、全材料一括PDF、Android/iOS固有の共有プラグインは今回追加していない。

## 操作

1. 対象の材料を選び、計算する。入力変更後なら再計算する。
2. 結果の「切断作業表（印刷・PDF）」を押す。
3. プレビュー下部の「PDF保存・共有」を押す。
4. 「PDFの準備ができました」の後、「PDFを保存」または「共有先を選ぶ」を押す。
5. 保存先はブラウザの設定に従う。保存先を毎回選ぶ設定でなければ通常はダウンロード一覧から開ける。

共有APIにはユーザーのクリックと対応環境が必要なため、PDF生成後に改めて共有ボタンを押す設計。LAN内のHTTP接続などでは直接共有が使えないことがある。その場合でもブラウザでPDFを保存して添付する導線を残す。
仕様根拠：[W3C Web Share](https://www.w3.org/TR/web-share/)。

## 今回変更したファイル

- `src/routes/index.tsx`：作業表ボタンの表示、プレビューからPDF作成への導線。
- `src/components/cutting-order-pdf-dialog.tsx`：作成進捗・保存・共有・キャンセル・失敗案内。
- `src/lib/cutting-order-pdf.ts`：紙面を独立した画面へ複製して1ページずつPDF化し、終了時に後片付けする。
- `src/lib/cutting-order-export.ts`：ファイル名、保存、共有可否と失敗処理、用紙からのはみ出し判定。
- `src/lib/cutting-order-export.test.ts`：PDF関連の8テスト。
- `src/styles/cutting-order-paper.css`：従来の印刷用ルールを移動してPDFと共有。ルール自体は変更しない。
- `src/styles.css`：共有した紙面スタイルを印刷時に読み込む。
- `package.json` / `package-lock.json`：既存の間接依存と同じhtml2canvas 1.4.1・jsPDF 4.2.1を直接依存に明記し、テストを追加。
- `CUTTING_PDF_CHECK.md`：この記録。

作業開始時からあった未コミット変更（標準計算、材料一覧、旧端材バンク互換など）は維持。今回、切断最適化・保存形式・見積計算は変更していない。コミット・外部への反映はしていない。

## 確認済み

- `npm test`：110件成功（既存102件＋今回8件）。切断最適化、500本、保存復元、見積データ保持、取り込み等を含む。
- `npx tsc --noEmit`：成功。
- 変更したTypeScript/TSXのESLint：成功。
- `npm run lint`（プロジェクト全体）は長時間完了しなかったため中断。代わりに`npx eslint src`を実施したところ、今回未変更のファイルに整形エラー7件・既存警告18件があった。エラーは日付付きの`cutting-stock`複製2ファイルと`src/routes/__root.tsx`で、今回の範囲外として変更していない。全体lint成功とは扱わない。
- `npm run build`：成功。PDFライブラリは必要になった時に読み込む別ファイルになっている。
- `git diff --check`：成功。
- 専用のlocalhost:8092で、計算済みデータからプレビューを開き、PDFを作成・ダウンロードできた。普段使うlocalhost:8080のデータには触れていない。
- PDF作成中のキャンセルでプレビューへ戻り、その後も作成し直せた。
- 保存ボタンでは「保存を開始しました」と案内し、保存先への書き込み完了までは断定しない。
- 共有APIの非対応、例外、ユーザーキャンセル、成功、ファイルだけを渡す動作を自動テストで確認。実際の共有先への送信は行っていない。
- 500本（1,200mm×500、6,000mm定尺、刃厚4mm、パイプ番号と日本語名あり）を実際のPDFへ出力。125枚の定尺カード、500個のチェック欄、4ページ、661,386バイト（約0.63MiB）。
- PDFを全ページ画像化して確認。A4横、4列、日本語、寸法、チェック欄、ページ番号、最終カード#125を保持。ページ端での見切れなし。
- 初回確認で見つかった文字の下方ずれを修正。原因はPDF描画ライブラリが文字位置の測定に使う非表示画像への既存CSSの影響。測定用画像だけに一時的な補正を行い、通常のUI画像や従来印刷には変更を加えない。
- 検証後、専用ブラウザの入力を元の保存済みテスト案件へ戻した。普段の案件履歴には追加・上書きしていない。
- 検証用タブ・8092の検証サーバーを終了し、生成した確認用PNGを削除した。ダウンロードした確認用PDFはダウンロードフォルダーに残している。

## 後日、実機で確認すること

1. ブラウザを更新して、保存済み案件を開く。
2. 材料を計算し、上記の操作でPDFを保存する。
3. 保存したPDFを開き、案件名、材料・規格、寸法、切断総数を確認する。
4. 複数ページなら、2枚目以降の材料名・規格名・ページ数と、最後のカードを確認する。
5. 共有ボタンが使える端末では共有先一覧を開き、一度キャンセルする。入力内容が変わらないことを確認する。
6. その後、自分宛てなど確認できる相手へ共有し、受信側でPDFを開けることを確認する。
7. 共有が使えない場合はPDFを保存し、普段使うメール等へ添付する。
8. 従来の「印刷画面を開く」からも印刷できることを確認する。

## 辛口レビュー・制約

- ブラウザ上の保存とPDF本体は確認済みだが、Android/iPhone実機・インストール版アプリでの保存共有は未確認。実機共有まで「完成確認済み」とは扱わない。
- PDFは約192dpiの画像方式。印刷用の日本語レイアウトを保持しやすい一方、文字検索・コピー・読み上げ用のテキストは含まない。大きく拡大すると画像の限界がある。
- 既存の安全寄りの改ページを引き継ぐため、紙面に余白が残る場合がある。今回の500本例では最終4枚目が1カードだけ。PDF追加と同時に改ページロジックまで変更していない。
- 1ページずつ描画してメモリを抑えるが、大量の部材・低性能端末では時間がかかる。500本の全条件・全端末での速度を保証するものではない。
- 実際の紙面サイズが用紙からはみ出す場合は、見切れたPDFを保存させずエラーにする。極端に長い名称や1枚に収まらない定尺カードを自動分割する機能は今回含めない。
- PDF用の約600kBの別ファイルについてビルドのサイズ警告が出る（圧縮転送時約175kB）。初回表示には読み込まず、PDF作成時だけ読み込む。既存のVite設定の移行推奨警告も残る。
- このPDFは切断作業用であり、見積の単価・金額・備考は含めない。案件保存の内容には影響しない。
