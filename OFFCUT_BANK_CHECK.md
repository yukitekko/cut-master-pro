# 材料一覧・端材バンク：実装・確認メモ（2026-08-31）

このメモは旧仕様の記録です。その後の方針変更で在庫関連の操作は通常画面から外し、保存データは保持しています。現在の仕様・確認結果は [STANDARD_PLANNING_CHECK.md](./STANDARD_PLANNING_CHECK.md) を参照してください。

## 今回の更新：共通の材料・規格一覧

- 「＋ よく使う材料・規格を登録」で名前と規格をセット登録。案件の材料選択と端材登録で同じプルダウンを使う。
- 登録IDで紐付ける。案件内の材料行ID・作業IDとは分離し、複製しても材料名に「コピー」を付けない。材料タブには行番号と規格を表示する。
- 材料選択のすぐ下に使える端材・在庫本数・場所を表示。その場でチェックし、使用上限本数を指定できる。
- 別の材料・規格に切り替えると端材の選択を解除する。部材寸法・本数・定尺・刃厚は消さない。
- 「定尺を購入する」「手持ちの定尺を使う」と明記し、端材バンクとは区別する。
- 切断完了前に、端材と手持ち定尺それぞれの「更新前 − 使用 ＋ 登録 → 残り」を表示。新しい完了記録にはこの内訳も保存する。古い完了記録の更新前本数は推測して表示しない。
- 材料一覧は端材と同じ保存データに追加し、一括保存する。旧端材は同じ名前・規格ごとに安定したIDを付けるが、読み込みだけでは保存データを上書きしない。旧案件は既知の組合せだけ紐付け、計算済み／再計算待ちと見積内容を保持する。
- 共通の定尺・単価・仕入先設定、材料一覧の改名・削除・統合は追加していない。単発の材料は従来の手入力も可能。

## 今回追加したこと

- 画面上部の「端材バンク」から手元の端材を登録。材料・規格を選択し、実測長さ・本数を入力。保管場所は任意。
- 同じ登録材料の端材だけを、今回使える上限本数を決めて選択。
- 選んだ端材を先に使い、その後で従来の手持ち定尺・購入材の計算につなぐ。端材の長さを追加購入候補にはしない。
- 結果・切断カードに「端材／手持ち／購入」を表示。既存の4列印刷を維持。
- 「切断完了・端材を残す」で、残す端材にチェックし、実測長さと保管場所を確認して登録。
- 計算・再計算・選択・キャンセルでは在庫を変更しない。完了時だけ使用した端材と案件内の手持ち定尺を減算。
- 在庫本数と完了記録を一括保存。同一作業の二重更新を拒否し、完了済みの条件を固定。別作業は材料・案件を複製して再計算する。
- 古い保存案件でも完了記録から残数・計画・対応する見積材料行を復元。単価や備考の保存機能は維持。
- 端材の実数が違うときは「全材料の端材・実数の修正」で修正。0本は在庫なし。

## 確認結果

- 自動テスト：89件成功（端材バンク導入前56件＋端材バンク18件＋今回15件）。
- 型チェック：成功。
- 変更したTypeScript／TSXファイルのlint：エラー・警告なし。リポジトリ全体のlintは実施していない。
- 本番ビルド：クライアント・サーバー・ページの事前生成まで成功。
- 差分の空白チェック：成功。Gitの改行コードの注意表示はあり。
- Vite既存設定の案内とビルド処理時間の警告は残っている。今回は設定や依存パッケージを変更していない。

自動テストで確認した主なケース：

- 端材なしの結果が既存計算と一致する。
- 端材の本数を超えず、不足分を正規の定尺だけで補う。
- 同寸法でも別保管場所の端材IDを保持する。
- 500本の寸法・番号・総数・切断カード・在庫上限を維持する。
- 異なる材料・規格、足りない在庫、古い計算、過剰な端材登録を拒否する。
- 完了時の減算・選択した端材だけの登録・二重更新防止。
- 別案件で先に同じ端材を消費した場合の拒否。
- 保存失敗で在庫を部分更新しない。壊れた端材データを空扱いで上書きしない。
- 古い案件・下書きからの完了記録、見積の単価・任意本数・備考の復元。

ブラウザ画面で確認済み：

- 今回は検証用の `localhost:8092` を使用。普段使う `localhost:8080` の案件・端材は操作していない。
- 旧案件のSGP／150Aが登録材料へ紐付き、元の1830mm端材1本と計算済み状態を保持する。
- 材料を複製してもSGP／150Aのまま、同じ端材が候補になる。端材の選択自体は複製しない。
- SGP／100Aの登録が案件・端材登録の両方の一覧に反映される。100Aの1830mm端材2本を登録しても、150Aの1830mm端材1本と混ざらない。
- プルダウンで100Aから150Aへ選び替えると、100Aの端材選択が解除されて再計算待ちになる。
- 材料2種類を計算・案件保存し、検証タブを閉じて新しいタブで開き直しても材料一覧・選択・両方の計算済み状態が復元される。以前入力した単価12345・500と備考も保持する。
- 完了前プレビューに端材1830mmの「1 − 1 → 0」、定尺6000mmの「1 − 1 → 0」を表示。2380mmを棚Bに残す選択で「0 − 0 ＋ 1 → 1」が追加される。キャンセル後も元の在庫1本が残る。
- 端材登録、選択、端材・手持ち・購入の内訳、切断順プレビュー。
- 切断完了確認画面のキャンセル後も、元の端材が1本残る。
- タブを閉じて開き直した際、定尺単価12345・端材単価500・入力した備考が復元される。
- 更新前からある保存案件の定尺5000と計算済み状態、単価12345を保持して開ける。

未確認：

- ブラウザ上での「切断完了して在庫更新」の確定、その後の画面復元。二画面からの確定テストは在庫変更の安全保護で停止したため、代替の確定操作はしていない。更新・復元のロジックは上記の自動テストで検証した。
- 今回の変更後の実プリンター出力、Android実機、狭い実機画面での操作。

## 手動確認手順（実在庫ではなくテスト用の材料で実施）

1. 開発中のアプリを更新。現在の大事な案件は先に保存してから、新しいテスト案件を作る。
2. 案件名を「端材テスト」にする。「＋ よく使う材料・規格を登録」で材料名「端材テスト材」・規格名「テスト150A」を登録して選ぶ。
3. 「手持ちの定尺を使う」で定尺6000mm・1本、刃厚4mm、部材P-01を1200mm・8本にする。
4. 「端材を登録・確認する」→「手元の端材を登録」で、同じ登録材料を選んで1830mm・1本を「テスト棚A」に登録する。
5. 閉じたら材料選択の下にある1830mm端材にチェックし、使用上限1本にする。計算後、端材1本・手持ち6000mm材1本・購入6000mm材1本になることを確認する。
6. 見積の6000mm材単価を12345、1830mm材単価を500、備考を「端材復元テスト」にして案件保存する。
7. 「切断完了・端材を残す」を開き、端材と手持ち定尺それぞれが「更新前1本 − 使用1本 → 残り0本」と表示されることを確認。一度キャンセルし、実際の在庫は減っていないことを確認する。
8. もう一度開き、2388mmの候補だけにチェック。実測長さを2380mm、保管場所を「テスト棚B」として「切断完了して在庫更新」を1回実行する。これはテスト上の完了操作であり、実際の材料を切る必要はない。
9. 案件内の手持ち定尺が0本、元の端材が0本、新しい2380mmの端材が1本になることを確認する。完了ボタンは更新済みになり、計算条件は固定される。
10. 同じブラウザ・同じアドレスで開き直す。完了状態・残数・切断結果・見積単価・備考が戻ることを確認する。手順6の古い保存案件を開いても在庫が二重に減らないことを確認する。
11. 材料を複製し、材料選択が同じ「端材テスト材／テスト150A」のまま、2380mmを選び直せることを確認する。複製自体では在庫は減らない。
12. 「テスト100A」も登録。プルダウンで切り替えると、150Aの端材が表示・選択されないことを確認。定尺や部材の寸法は切替では消えないので、その材料に合う条件か確認する。

テスト用端材を在庫から外す場合は、全材料の一覧から「実際の在庫本数」を0にする。ブラウザの保存データ全体は削除しない。

## 辛口レビュー・今回の範囲

- 一覧を選ぶことで新たな表記揺れは減るが、過去の「SGP」「ＳＧＰ」等は勝手に同じ材と判断・統合しない。登録時に名前・サイズ・肉厚等を正しく区別する必要がある。登録の改名・削除・統合画面は未実装なので、まずよく使う少数の材料で確認する。
- プルダウンを作っても、在庫が減るのは「切断完了」の確定時だけ。計算・印刷は在庫を更新しない。今回の事前表示でその違いを見えるようにした。
- 端材優先の計画であり、購入費が最小になる保証はない。価格を使った最適化は未実装。
- 定尺の手持ち数は案件内の入力値。会社全体の共通在庫台帳ではない。共通管理するのは端材バンクだけ。
- 選択時の取り置きはしない。複数案件で同じ端材を計画に含められるため、実物と在庫の確認は必要。実際の更新時は最新在庫を読み直す。
- 途中までの完了・完了の取り消しは未実装。実運用では計画どおり全部切った時だけ完了する。
- 既存計算の端材表示は部材間の刃厚を引いた値。バンクの登録候補は、さらに最後の切り離し分の刃厚を引いている。候補値は目安なので実測で確認する。
- 保存先は同じブラウザ・同じアドレス。端末間同期・クラウド・PDF共有・バックアップは今回追加していない。大量の完了記録で保存領域がいっぱいになると、更新は失敗として通知される。
- 複数タブの更新を直列化できないブラウザ環境では、安全のため在庫更新を拒否する。localhostまたはHTTPSの対応ブラウザで使用する。
- 機能を追加し続けるより、上記の実画面での完了確認とAndroid実機確認を先に済ませるべき段階。

## 変更ファイル

- `src/lib/material-catalog.ts`：材料名・規格の登録、ID紐付け、選択切替時の端材解除。
- `src/components/material-picker.tsx`：案件・端材登録で共通の材料プルダウンと登録フォーム。
- `src/components/inventory-changes.tsx`：更新前・使用・登録・残りの表示。
- `src/lib/offcut-bank.ts`：端材保存、在庫検証、完了記録、一括更新と復元。
- `src/lib/offcut-planning.ts`：端材優先計算と登録候補寸法。
- `src/components/offcut-dialogs.tsx`：登録・選択・実数修正・切断完了の画面。
- `src/routes/index.tsx`：既存画面・保存・計算・印刷との接続。
- `src/lib/project-storage.ts`：作業ID・選択端材の保存と再計算判定。
- `src/lib/cutting-stock.ts`：端材区分の型だけ追加。既存最適化アルゴリズムは変更していない。
- `src/lib/offcut-bank.test.ts`：端材バンク18件に、材料一覧・移行・事前確認15件を追加。
- `package.json`：追加テストを通常のテストに含める。
- `OFFCUT_BANK_CHECK.md`：この確認メモ。

端材バンク初回の作業開始時は未コミット変更なし。今回の材料一覧の作業開始時には、上記の端材バンク関連ファイルが未コミットだったため、その内容を確認して引き継いだ。既存の8件の未送信コミットも保持。今回の更新対象は新規3ファイルと `project-storage.ts`、`offcut-bank.ts`、`offcut-bank.test.ts`、`offcut-dialogs.tsx`、`index.tsx`、この確認メモ。`cutting-stock.ts` と `package.json` の未コミット差分は前回分で、今回は変更していない。公開先への送信もしていない。
