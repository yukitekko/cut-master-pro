import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yukitekko.cutmaster',
  appName: 'Cut Master Pro',
  webDir: 'dist/client',
  
};

export default config;
