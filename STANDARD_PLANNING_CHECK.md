# 定尺の必要総本数に絞る変更（2026-08-31）

## 変更内容

- 通常画面から購入／手持ちの切替、手持ち定尺の本数入力、端材選択、端材バンク、切断完了による在庫更新を外した。
- 定尺の長さ・刃厚・必要な部材だけから、必要総本数と切り方を計算する。同じ材料・規格・長さの手持ち分は、利用者が総本数から差し引いて発注する。
- 材料・規格の選択、案件の保存・履歴・複製・下書き、複数材料、Excel／CSV取り込み、見積、4列切断カードは維持。
- 見積の材料本数は必要総本数を基準にする。発注本数と見積本数を自動で連動させない。再計算では、同じ定尺長の見積単価を引き継ぐ。

## 保存データの扱い

- 既存の案件・端材バンクを一括削除・一括上書きしない。旧在庫関連のデータ構造と互換処理も残す。
- 旧在庫計画は保存結果を表示したまま再計算待ちにする。再計算するまで印刷・見積作成を無効にし、新方式の計画と混同させない。
- 旧完了記録がある案件は初回読み込み時に整合を取り、その後の下書きは在庫更新から切り離す。編集した寸法や単価が再起動時に昔の完了記録へ戻らないようにする。
- 新規／開いた案件の材料に `planningMode: "standard"` を持たせる。通常の計算は旧数量・端材を参照せず、端材バンクへ書き込まない。
- 旧購入方式で、在庫条件を使っていない有効な計算はそのまま利用可能。
- 材料一覧は独立した保存先 `cut-master-pro:material-catalog:v1` を使用する。まだ無ければ旧端材バンクの一覧を読み取り、新しい登録時にIDを保って移す。旧バンク自体は変更しない。
- 材料一覧が壊れていても、手入力で計算できる。壊れた保存データを空の一覧で上書きしない。

## 今回変更したファイル

- `src/routes/index.tsx`：在庫関連の操作を除去、必要総本数の表示、旧案件復元、材料一覧の独立保存。
- `src/lib/project-storage.ts`：標準計算を表す項目と、旧在庫数量を無視する再計算判定。
- `src/lib/offcut-bank.ts`：標準計算の案件を古い完了記録で上書きしない／在庫更新対象にしない。
- `src/lib/standard-planning.ts`（新規）：在庫に依存しない計算・入力検証・互換復元。
- `src/lib/material-catalog-storage.ts`（新規）：材料一覧の独立保存と読み取り移行。
- `src/lib/standard-planning.test.ts`（新規）：今回の回帰テスト13件。
- `package.json`：テスト対象を追加。
- この確認メモ、および `OFFCUT_BANK_CHECK.md` 冒頭の旧仕様の注記。

作業前からあった未コミット変更・未追跡ファイルは保持した。今回、最適化本体 `cutting-stock.ts` と印刷のページ分割・スタイルは変更していない。同ファイル等に見える既存差分を取り消していない。コミット・push・公開は未実施。

## 確認結果

- 自動テスト：102件成功（既存89件＋今回13件）。
- 型チェック：`npx tsc --noEmit` 成功。
- 今回変更したTypeScript／TSXのeslint：エラー・警告なし。リポジトリ全体のlintは未実施。
- 本番ビルド：`npm run build` 成功。従来のViteのパス解決プラグイン警告と処理時間の通知は残るが、ビルドエラーなし。
- `git diff --check`：成功。
- 500本・複数定尺の計算、部材番号／空欄名称、カードへの切断総数の引き継ぎを自動テストで確認。
- 旧案件の結果と見積保持、旧完了記録から復元後の編集／保存／再読込、端材バンクの内容不変、破損データ・保存失敗を自動テストで確認。
- ブラウザ操作：普段の8080とは別のテスト用8092を使用。実際の8080の保存データには触れていない。
  - 旧案件のSGP 150A／100Aを開き、両方が再計算待ちになること。
  - 両材料を再計算し、6000mmが各2本となり、端材や手持ちの区分が消えること。
  - 登録材料のIDを保って一覧を保存できること。
  - 150Aの既存単価12345円を保持。100Aに6789円、備考に「標準計算に変更後も単価と備考を保持」を入力。
  - 保存案件を開き、タブを閉じて新しいタブで再表示しても、両材料の計算済み状態・単価・備考が戻ること。
  - 切断順プレビューに6000mmの定尺カード2枚、P-01を各4本、切断総数8本、1本ごとのチェック欄を表示。
  - 定尺6000→5000の変更で再計算表示・印刷無効化。案件履歴を開き直すと保存時の6000へ復元。

今回はアプリ内プレビューとタブの閉じ直しまでを確認。紙への実印刷・Android実機・ブラウザプロセス全体の再起動は未実施。

## 手動確認手順

1. 普段使っている同じブラウザ・同じURLの画面を更新する。
2. 購入／手持ち切替、手持ち本数、端材バンク、切断完了ボタンが無くなり、材料・規格のプルダウンが残っていることを確認する。
3. 旧在庫案件を開いた場合は表示を確認し、定尺の長さを見直して材料ごとに「再計算する」を押す。
4. 例：定尺6000mm、刃厚4mm、部材1200mm×8本で必要総数が2本になることを確認する。同条件の定尺を1本持っているなら、購入するのは自身で差し引いた1本。
5. 切断順プレビューで切断総数・番号・寸法・チェック欄を確認し、必要なら試し印刷する。
6. 見積の単価・備考を入れ、案件保存。ブラウザを再起動し、同じURLで結果・見積を確認する。開発サーバーは動かしたままにする。

## 辛口レビュー・割り切り

- 在庫をほとんど抱えない運用では、在庫入力や消込を必須にするより、この流れの方が操作負担が小さい。ただし必要総本数と実際の購入本数は別物。画面に明記した。
- 本数に限りのある短い端材を定尺欄へ入れると、必要なだけ調達できる長さとして計算してしまう。今回この用途は対応しない。注意文を付け、旧選択端材の長さを定尺へ自動転用しない。
- 表示される端材長さは計算値。実測との差・切り損じを自動補正するものではない。現物確認は引き続き必要。
- 互換性のため旧在庫コードは残る。今回まとめて削除するより安全だが、全案件の移行と運用確認が済んだ段階で整理する余地がある。
- 次に機能を足す前に、普段の1案件をこの入力・計算・印刷の流れで通して確認するのがよい。
